13 Refactor: Creacion de rutas GET y POST 


Se agrega el enrutamiento de los metodos necesarios para que la API cumpla
su funcion de validar los usuarios y guardar los registerID.

Se modifica el archivo gitignore.txt por .gitignore para que no suban 
los modulos ni los logs que se originan en las ejecuciones.