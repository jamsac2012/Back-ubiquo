14 Creación de BD NoSQL con RethinkDB para la persistencia:

Se agrega el modulo de conexión con la BD en /model/config.js.
Este módulo será utilizado desde en el archivo server.js para 
persistir los datos en el metodo POST de la API.